# include <string.h>
# include "main.h"

/**
 * _strlen - function that returns the length of a string.
 * @s:string
 * Return:intger number length of a string.
 */

int _strlen(char *s)
{
	return (strlen(s));
}
