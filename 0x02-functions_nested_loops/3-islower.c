#include "main.h"

/**
 * _islower - a function that checks for lowercase character
 *
 *
 * Return: 1 if c is lowercase and 0 lese
*/

int _islower(int c)
{
	int i = c;

	for (i = 97; i <= 122; i++)
	{
		return (1);
	}

	return (0);
}
