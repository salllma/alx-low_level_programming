#include<stdio.h>

/**
 * main - entry point to a function that print the alphabet in lowercase
 *
 * Return: Always 0 on success
*/

int main(void)
{
	int i = 0;

	for (i = 97; i <= 123 ; ++i)
		printf("%c", i);


	return (0);
}
